version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-marco
    ports:
      - '3001:3000'
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
